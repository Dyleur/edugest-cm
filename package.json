{
  "name": "edugest",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd edugest-backend && npm run dev",
    "dev:frontend": "cd Front_final && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "pdfkit": "^0.19.1"
  }
}
